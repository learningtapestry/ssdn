/**
 * upload-credentials-form.ts: Interface that represents the parameters used in a form to generate
 * temporary credentials
 */

export default interface UploadCredentialsForm {
  client: string;
  format: string;
}
